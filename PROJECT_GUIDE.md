# Toonflow Custom 项目指南

Toonflow 是把小说/剧本转为角色与场景资产、分镜图、动态视频的单一工作台。当前自定义分支开始加入可恢复的自动生产底座；用户仍在 Toonflow 内操作，不需要启动参考项目。

## 最常用流程

1. 创建项目，选择画风、图片模型、视频模型和横竖屏比例。
2. 导入小说或新增剧本，由剧本 Agent 生成/整理内容。
3. 提取角色、场景、道具，审核提示词并生成资产图。
4. 由生产 Agent 制定导演方案并生成分镜，人工审核关键镜头。
5. 在制作工作台单条或批量生成视频。两种入口都先进入持久队列，默认合计最多并行 2 个。
6. 选定可用视频。自动配音、时间线合成和 QA 尚在后续吸收批次中。

## 任务异常怎么处理

- `等待重试`：付费提交前的暂时错误，系统会有限次重试。
- `等待重试`且已有远端任务编号：系统稍后继续查询同一个供应商任务，不会重新提交。首个支持者是普通火山引擎供应商。
- `需人工确认`：供应商尚未支持恢复协议，或提交成功但任务编号未能写入。先在供应商后台确认后才能重试。
- `已取消`：排队任务已直接取消；供应商调用中的任务只能在当前调用返回后收口，首版还不能远端中止所有供应商。

详细说明从 [知识中心](docs/knowledge-hub/README.md) 开始。持续吸收其他项目见 [参考上游说明](docs/upstream-watch/README.md)。
